"""Signature-library generation. Not imported at plugin load.

These modules build the .warp libraries in ../signatures from IDR knowledge
bases; they are shipped so the libraries can be rebuilt and audited, not
because the plugin needs them at runtime.
"""
