# Test binaries, fetched not authored -- 250 MB and not ours to redistribute
corpus/
corpus-fpc/

# Build scratch and analysis databases
work/
out/
*.bndb
*.bin

__pycache__/
*.pyc

# Signature libraries are generated, but the ones under signatures/ are the
# plugin's shipped payload and belong in the repository.
*.warp
!signatures/*.warp

# FPC research: upstream compiler sources and derived datasets, kept here so
# the material lives with the project rather than in a temp directory. Not
# ours to redistribute, and inputs rather than deliverables.
fpc-research/
